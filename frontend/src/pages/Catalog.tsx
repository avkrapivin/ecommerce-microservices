import { Card, Typography } from 'antd';

export function Catalog() {
  return (
    <Card>
      <Typography.Title level={4}>Catalog</Typography.Title>
      <Typography.Paragraph>Product list with filters will be here.</Typography.Paragraph>
    </Card>
  );
}
